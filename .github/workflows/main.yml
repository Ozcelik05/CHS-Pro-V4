name: Android Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build APK
      run: |
        echo "APK build başlıyor..."
        ls -la

    - name: Upload APK
      uses: actions/upload-artifact@v2
      with:
        name: app-files
        path: "."
